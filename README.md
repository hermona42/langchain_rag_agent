# LangChain RAG Agent

This project contains the basic folder structure for a LangChain-based retrieval-augmented generation agent.

## Structure

- `config/` for settings
- `src/` for application code
- `tests/` for pytest coverage

## Run

```bash
python main.py
```
